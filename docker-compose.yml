services:
  app:
    build: .
    env_file: .env
    mem_limit: ${MemoryLimit}
    command: >
      bash -c "./omp-server"
    ports:
      - ${Port}:${Port}/udp
    environment:
      - TZ=${TZ}
    volumes:
      - ./scripts/sqlite:/app/scripts/sqlite
      - ./logs:/app/logs