services:
  app:
    build: .
    env_file: .env
    mem_limit: ${MemoryLimit}
    ports:
      - ${Port}:${Port}/udp
    environment:
      - TZ=${TZ}
    volumes:
      - ./scripts/sqlite:/app/scripts/sqlite
      - ./logs:/app/logs